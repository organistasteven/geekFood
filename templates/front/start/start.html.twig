{% extends 'base.html.twig' %}

{% block title %}GeekFood Entrées{% endblock %}

{% block body %}
   <div class="title">
        
        <h1>Entrées</h1>
        
    </div>
    <main>
        <div class="list">
        {% for key, start in starts %}
            <span class="foodHead">
                <!--icon-->
                <img class="foodIcon" src="{{ asset('' ~ start.icon ~ '') }}" >
                <!--name-->
                
                <h4>{{ start.name }}</h4>
                <!--price-->
                <p>{{ start.price }}$</p> 
              <!--image-->
                <img class="foodPic" src="{{ asset('' ~ start.image ~ '') }}"  >
            </span>
            <!--description-->
            <p>{{ start.description }}.
            </p>
            <!--ingredients-->
            <p class="content">{{ start.content }}. </p>
            <br>

             {% endfor %}
             </div>
                <div class="nav">
            
           
            <section>
                <h4><a href="{{ path('app_carte')}}">La carte</a></h4>
                 <li><a href="{{ path('app_start')}}">Entrées</a></li>
                <li><a href="{{ path('app_plat')}}">Plats</a></li>
                <li><a href="{{ path('app_dessert')}}">Desserts</a></li>
                <li><a href="{{ path('app_drink')}}">Boisons</a></li>
                <li><a href="{{ path('app_menu')}}">Menus</a></li>
            </section>
        
        </div>
{% endblock %}
