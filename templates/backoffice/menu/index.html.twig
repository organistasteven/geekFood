{% extends 'baseback.html.twig' %}

{% block title %}Menu index{% endblock %}

{% block body %}
<div class="backshow">
    <h1>Liste des menus</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Content</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for menu in menus %}
            <tr>
                <td>{{ menu.id }}</td>
                <td>{{ menu.name }}</td>
                <td>{{ menu.content }}</td>
                <td>{{ menu.price }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('app_menu_show', {'id': menu.id}) }}">Détail</a>
                    <a class="btn btn-warning" href="{{ path('app_menu_edit', {'id': menu.id}) }}">Edition</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('app_menu_new') }}">Creation d'un nouveau menu</a>
    </div>
{% endblock %}
