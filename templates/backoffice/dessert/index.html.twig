{% extends 'baseback.html.twig' %}

{% block title %}Dessert index{% endblock %}

{% block body %}

    <h1>Dessert index</h1>

    <table class="table" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Icon</th>
                <th>Image</th>
                <th>Content</th>
                <th>Price</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dessert in desserts %}
            <tr>
                <td>{{ dessert.id }}</td>
                <td>{{ dessert.name }}</td>
                <td>{{ dessert.description }}</td>
                <td>{{ dessert.icon }}</td>
                <td>{{ dessert.image }}</td>
                <td>{{ dessert.content }}</td>
                <td>{{ dessert.price }}</td>
                <td>{{ dessert.type }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('app_dessert_show', {'id': dessert.id}) }}">Détail</a>
                    <a class="btn btn-warning" href="{{ path('app_dessert_edit', {'id': dessert.id}) }}">Edition</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('app_dessert_new') }}">Ajouter un nouveau dessert</a>
 
{% endblock %}
