{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
<div class = "backshow">
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ message.name }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ message.mail }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ message.content }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ message.status }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_message_index') }}">Index</a>

    <a class="btn btn-warning " href="{{ path('app_message_edit', {'id': message.id}) }}">Mis à jour du message</a>

    {{ include('backhome/message/_delete_form.html.twig') }}
    </div>
{% endblock %}
